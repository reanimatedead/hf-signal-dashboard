venv/
__pycache__/
*.pyc
.env
.DS_Store
*.egg-info/
dist/

# macro-environment (feat/macro-environment) — additive
hf-data-store/
data/macro.json
verify_report.json
seed/public/data.json
node_modules/

# Phase 1 — survival loop: 個別損益/残高は公開リポに置かない
config.local
config.local.*
!config.local.json.example
.pytest_cache/

# Phase 1.6 — notify: 個別通知ログとローカルストレージは公開しない
data/local/
~/Library/LaunchAgents/com.hf.notify.plist

# セッション用の一時権限設定 (出荷モード等) は誤commitを恒久防止
.claude/settings.json

# A-Ⅱ — 生成物は git に積まず、CI(deploy.yml) が Pages へ直接配信する。
# docs/data/macro.json (build_macro のレガシー出力) は判断保留につき追跡下に残す。
docs/data.json
docs/charts/
docs/health.json
docs/macro_v2.json
docs/relations.json
docs/gamma.json
# Task 5-4: link 死活の診断出力（CI 再生成・警告のみ）
docs/link_check.json

# Task 5-3: decisions.jsonl は「生成物ではなく記録」。追跡下に置く（無視しない）。
!docs/decisions.jsonl
